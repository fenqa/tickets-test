import plane from '../../assets/plane-header.png';

const Header = () => {
  return (
    <header className='w-full py-20'>
      <img className='m-auto' src={plane} alt='' />
    </header>
  );
};

export default Header;
